import { MainTitle, StyledHeader } from "./styles";

const Header = ( ) =>{
    return (
        <>
            <StyledHeader>
                <MainTitle>Todo hay Shop</MainTitle>
            </StyledHeader>
        </>        
    )
}

export default Header;